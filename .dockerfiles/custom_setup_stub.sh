#!/bin/bash
#
# This stub is only there to prevent errors when not using the
# MAKE4PY_DOCKER_SETUP_SCRIPTS variable of .make4py. It satisfies
# the COPY custom_setup_*.sh /tmp command of the Dockerfile.
